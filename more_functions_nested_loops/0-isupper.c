#include "main.h"
/**
 * _isupper - _isupper to show if character is uppercase
 * Return: On success 1.
 * @c: integer
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
