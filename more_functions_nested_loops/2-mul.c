#include <ctype.h>
#include <stdio.h>
/**
 * mul - mul to multiply two integers
 * Return: On success 1.
 * @a: integer
 * @b: integer
 */
int mul(int a, int b)
{
int sum;
sum = a * b;
return (sum);
}
