#include "main.h"
/**
 * _strlen - _strlen to calculate length of a string
 * Return: 0.
 * @a: integer
 * @b: integer
 */
int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{
}
return (i);
}
