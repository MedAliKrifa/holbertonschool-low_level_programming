#include "main.h"
/**
 * print_sign - print_sign if positive or negative or 0
 * Return: On success n.
 * @n: integer
 */
int _abs(int n)
{
if (n < 0)
{
n = (-1) * n;
return (n)
}
else
{
return (n)
}
return (0);
}
